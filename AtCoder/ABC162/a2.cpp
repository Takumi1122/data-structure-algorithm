#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() {
  char s[4];
  scanf("%s", s);
  if (s[0] == '7' || s[1] == '7' || s[2] == '7')
    puts("Yes");
  else
    puts("No");
}